import * as React from "react";
import * as ReactDOM from "react-dom";

export var Showcase = React.createClass({
    render: function() {
        return (
            <div id="showcase">
            </div>
        )
    }
});
