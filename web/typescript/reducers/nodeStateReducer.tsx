/*
export const nodeStateReducer = (state = { selectedNodes: [] }, action) => {
    switch (action.type) {
        case "select_nodes":
            return {
                selectedNodes: action.payload
            }
        default:
            return state;
    }
}
*/
